import { html, ReactiveElement } from "@gallop/gallop"

// component(`weben-panel`, function () {
//   return html``
// })

export function Panel(this: ReactiveElement) {
  return html` <div></div>`
}
